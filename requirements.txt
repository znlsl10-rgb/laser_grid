# 레이저 그리드 품질검측 — Isaac Sim 없이 도는 부분의 의존성
#
# eq1~eq6·영역분할·회귀검증은 numpy 만으로 돈다. 아래는 그 위의
# 산출물(이미지·엑셀)과 선택적 가속에 쓰인다.

numpy>=1.24
Pillow>=10.0          # 세그멘테이션 이미지·오버레이 렌더
openpyxl>=3.1         # 엑셀 조서 (report_excel.py)

# 선택 — 없으면 해당 단계가 성능이 떨어지거나 폴백으로 동작한다
scikit-learn>=1.3     # geom 백엔드의 DBSCAN 공간 분리
scipy>=1.10           # 가림 그림자 병합·밀도 추정의 KD-tree
